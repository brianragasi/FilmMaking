name: Deploy EcoCart to GoogieHost

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ecocart-production
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Build DaisyUI stylesheet
        run: |
          npm ci
          npm run build:css

      - name: Validate JavaScript
        run: |
          node --check assets/app-public.js
          node --check assets/app.js

      - name: Deploy runtime files over FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.GOOGIEHOST_FTP_SERVER }}
          username: ${{ secrets.GOOGIEHOST_FTP_USERNAME }}
          password: ${{ secrets.GOOGIEHOST_FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./
          server-dir: /public_html/
          timeout: 120000
          log-level: standard
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/tmp/**
            **/dist/**
            **/release/**
            **/installer/**
            **/*.log
            **/*.md
            **/*.pdf
            **/package.json
            **/package-lock.json
            **/assets/app.css
            **/assets/app-public.css
            **/assets/app.js
            **/assets/images/swarmgrid-c2-icon.png
            **/public/output.css
            **/database/**
            **/story.txt
            **/attacker-terminal.php
            **/index.html
            **/main.js
            **/preload.js
            **/includes/config.local.php
